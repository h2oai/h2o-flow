#file-open-persist-dialog.modal.fade(tabindex='-1', role='dialog', onload="Flow.FileOpenPersistDialog().onload();")
  .modal-dialog
    .modal-content
      .modal-header
        button.close(type='button', data-dismiss='modal')
          span ×
          span.sr-only Close
        h4.modal-title Open Persisted Flow...
        p(style='color:grey') Type * to list all.
      .modal-body
        input.flow-textbox(type='text' style='width:100%' data-bind="value:specifiedPath, valueUpdate: ['keydown', 'click'], typeahead: listPathHints" placeholder='Enter a flow name')
        // ko if:overwrite
        p(style='color:red') A notebook with that name already exists. Do you want to replace it?
        // /ko
      .modal-footer
        button.btn.btn-default(type='button' data-bind='click:decline') Cancel
        // ko ifnot:overwrite
        button.btn.btn-primary(type='button' data-bind='click:loadNotebook, enable:canAccept') Open
        // /ko

        // ko if:overwrite
        button.btn.btn-danger(type='button' data-bind='click:loadNotebook, enable:canAccept') Replace
        // /ko
